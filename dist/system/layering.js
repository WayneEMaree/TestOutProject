var layering = (function () {
    var layerStore = [];
    function init() {
    }
    function reorderLayer(targetIndex, destinationIndex) {
    }
    return {
        reorderLayer: reorderLayer,
        init: init,
    };
})();
layering.init();
//# sourceMappingURL=layering.js.map